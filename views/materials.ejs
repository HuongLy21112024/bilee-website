<%- include('layout_header') %>
        <main class="col-md-10 ms-sm-auto px-md-4">
            <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                <h1 class="h2">📂 Kho Tài liệu học tập</h1>
                <button class="btn btn-info text-white">📤 Tải lên tài liệu</button>
            </div>
            <div class="table-responsive bg-white shadow-sm p-3 rounded">
                <table class="table table-striped table-hover align-middle">
                    <thead class="table-secondary">
                        <tr>
                            <th>Tên tài liệu</th>
                            <th>Loại file</th>
                            <th>Dung lượng</th>
                            <th>Ngày tải lên</th>
                            <th>Thao tác</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (materials && materials.length > 0) { %>
                            <% materials.forEach(file => { %>
                            <tr>
                                <td><%= file.FileName %></td>
                                <td><span class="badge bg-secondary"><%= file.FileType %></span></td>
                                <td><%= file.Size %> MB</td>
                                <td><%= file.UploadDate %></td>
                                <td>
                                    <button class="btn btn-sm btn-primary">Tải về</button>
                                    <button class="btn btn-sm btn-danger">Xóa</button>
                                </td>
                            </tr>
                            <% }) %>
                        <% } else { %>
                            <tr><td colspan="5" class="text-center">Kho tài liệu đang trống</td></tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </main>
    </div>
</div>