<!DOCTYPE html>
<html>
<head>
    <title>SmartLearn Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .stat-card { border-radius: 15px; border: none; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
    </style>
</head>
<body>
    <div class="container py-5">
        <h1 class="text-center text-primary mb-5">HỆ THỐNG QUẢN TRỊ SMARTLEARN</h1>
        
        <div class="row mb-4">
            <div class="col-md-4">
                <div class="card stat-card p-4 text-center bg-white">
                    <h3 class="text-muted">Tổng Sinh Viên</h3>
                    <h2 class="display-4 text-danger font-weight-bold"><%= total %></h2>
                </div>
            </div>
            <% stats.forEach(s => { %>
                <div class="col-md-2">
                    <div class="card stat-card p-3 text-center bg-white">
                        <h6>Cơ sở <%= s._id %></h6>
                        <h4 class="text-success"><%= s.count %></h4>
                    </div>
                </div>
            <% }) %>
        </div>

        <div class="card stat-card p-4">
            <h4>10 Hoạt động mới nhất (Dữ liệu phân tán)</h4>
            <table class="table table-hover mt-3">
                <thead class="table-primary">
                    <tr><th>Mã SV</th><th>Chi Nhánh</th><th>Lượt Xem</th><th>Điểm</th></tr>
                </thead>
                <tbody>
                    <% recent.forEach(r => { %>
                        <tr>
                            <td><%= r.student_id %></td>
                            <td><span class="badge bg-info text-dark"><%= r.campus_code %></span></td>
                            <td><%= r.views %></td>
                            <td><strong><%= r.score %></strong></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
        <div class="text-center mt-4">
            <button class="btn btn-primary" onclick="location.reload()">🔄 Cập nhật dữ liệu từ Cluster</button>
        </div>
    </div>
</body>
</html>